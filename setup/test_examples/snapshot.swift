import FBSnapshotTestCase
import Nimble
import Nimble_Snapshots
import UIKit
@testable import ${POD_NAME}

class ${POD_NAME}SnapshotTests: FBSnapshotTestCase {

//    func test__snapshot() {
//        let sizes = ["Small": CGSize(width: 80, height: 32),
//                     "Medium": CGSize(width: 100, height: 32),
//                     "Square": CGSize(width: 80, height: 60)]
//        let view = UIView()
//
////        expect(view).to(recordDynamicSizeSnapshot(sizes: sizes))
//        expect(view).to(haveValidDynamicSizeSnapshot(sizes: sizes))
//    }
}
